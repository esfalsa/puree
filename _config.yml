url: "https://esfalsa.github.io/"
baseurl: "/puree"
title: "Purée"

sass:
  load_paths:
    - _sass
    - node_modules

exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules

plugins:
  - jekyll-seo-tag

permalink: /:categories/:year/:month/:day/:title
