url: "https://esfalsa.github.io/"
baseurl: "/puree"
title: "Purée"

sass:
  load_paths:
    - _sass
    - node_modules

include: [_data]

exclude:
  - package.json
  - package-lock.json
  - Gemfile
  - Gemfile.lock

plugins:
  - jekyll-seo-tag

permalink: /:categories/:year/:month/:day/:title
